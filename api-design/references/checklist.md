# API Design Checklist

Use this list during design or review:

- Are resource names clear and consistent?
- Are request and response shapes stable and predictable?
- Are optional and required fields obvious?
- Are validation failures easy to interpret?
- Are pagination and filtering patterns consistent?
- Can the contract evolve without breaking existing clients?
- Do error responses include enough context to act on?
